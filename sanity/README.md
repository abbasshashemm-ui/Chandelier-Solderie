# Sanity schema

Schemas live in `sanity/schemaTypes/` (product, collection, site settings).

Open `/studio` in the Next.js app after setting `NEXT_PUBLIC_SANITY_PROJECT_ID`. See the root README for webhook and env setup.
